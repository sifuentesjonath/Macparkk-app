import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DriverTransportPage } from './driver-transport';

@NgModule({
  declarations: [
    DriverTransportPage,
  ],
  imports: [
    IonicPageModule.forChild(DriverTransportPage),
  ],
})
export class DriverTransportPageModule {}
